Vagrant.configure("2") do |config|
  config.vm.box = "suchagoodguy/centos-7-lvm"
  config.vm.box_version = "1.0"
  config.vm.provider "virtualbox" do |v| 
    v.memory = 4096
    v.cpus = 2 
  end 
end